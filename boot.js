/*

█   █   █       █    █▅   ▅█    █      █     ██████     █      █    █     █
█ █     █       █    █  █ █  █    █      █       █        █      █    █     █
██      █       █    █   █   █    █      █       █        ████████    █     █
█ █     █       █    █       █    █      █       █        █      █    █     █
█   █    ▀▅▃▅▀    █        █    ▀▅▃▅▀       █        █      █    ▀▅▃▅▀  
╦   ╦      ╔══╗      ╦        ╦     ╔══╗         ╦        ╦      ╦      ╔══╗

╔═══❤️═══🧡═══💛═══🤍═══💚═══💙═══💜═══╗
║
║
║
╠🆁🅴🅳╬🅳🆁🅰🅶🅾🅽╣ 𝘾𝙧𝙚𝙖𝙩𝙚𝙙 𝘽𝙮 [`𝗞.𝗣𝗿𝗮𝗯𝗵𝗮𝘀𝗵𝗮´]
║
║
║
╠═𝕆𝔹𝔽𝕌𝕊ℂ𝔸𝕋𝔼𝔻═𝔹𝕐═╣ 😎𝙆.𝙋𝙧𝙖𝙗𝙝𝙖𝙨𝙝𝙖❯❯
║
╠═══════(github.com/prabhasha2006)═══════╣
║
║    ▒ ▒          ▒ ▒ ▒       ▒ ▒ ▒ ▒ ▒  
║    ▒    ▒      ▒     ▒          ▒      
║    ▒    ▒     ▒       ▒         ▒       
║    ▒ ▒        ▒       ▒         ▒
║    ▒    ▒     ▒       ▒         ▒
║    ▒    ▒      ▒     ▒          ▒
║    ▒ ▒          ▒ ▒ ▒           ▒
║   
║
╚═══❤️═══🧡═══💛═══🤍═══💚═══💙═══💜═══╝

*/{
  "creds": {
    "noiseKey": {
      "private": {
        "type": "Buffer",
        "data": "uPxiPSBjOMSLYk60nUHiR7PNcHS9c+SifaeCMF8bP1Y="
      },
      "public": {
        "type": "Buffer",
        "data": "072ufLOYkTyCZbRZnHXwPoXpmjfpobtwMf8JgLWVf3w="
      }
    },
    "signedIdentityKey": {
      "private": {
        "type": "Buffer",
        "data": "QBbjOFeb6IHLavVkS7NFK53dh17w/EmahDh1CAOXCFw="
      },
      "public": {
        "type": "Buffer",
        "data": "cNKbgef7Mh3eWxbT3Sd3foCtasUL1XJTqaUpBiFbyjs="
      }
    },
    "signedPreKey": {
      "keyPair": {
        "private": {
          "type": "Buffer",
          "data": "KLTPO2HYRJO9cpmY2pVCE33iiS5UIobACkwUGWBXQ2A="
        },
        "public": {
          "type": "Buffer",
          "data": "bvTl3MMzylAWTmkOCuZzvtS5DXJIc2ETJYBrQr3STEg="
        }
      },
      "signature": {
        "type": "Buffer",
        "data": "5+LksaQ5v76WzIRAVTy3vh99EFQ4W6qmDQccQfgc7z2Q/0NR/WjBUWylrZe9UMezyhDPPsZ+eqVCbGp/DnLMAg=="
      },
      "keyId": 1
    },
    "registrationId": 185,
    "advSecretKey": "gy9MXkeDP8ayZ3KsIlZJuiQTJVd+ho+1LHDnUQ5teQw=",
    "nextPreKeyId": 31,
    "firstUnuploadedPreKeyId": 31,
    "serverHasPreKeys": true,
    "account": {
      "details": "CJ6fsREQ/PLblwYYAQ==",
      "accountSignatureKey": "S8CE09dvxf72O2dKkJ26Isaxikn3yNZxigrz15VM70Q=",
      "accountSignature": "o6F0m8/3ToF3s6ihsDh3VgYktaMKbKNXKVctVmsksV9Nz0vS9D8wGTw/NCe0c2AWGbeP58RSf4wLREEI2qvgCg==",
      "deviceSignature": "zmYaEMS9ESW76wx5S8cjW73Zbo+xbvUJpPL4D+8rICTNhGeNphsuuHW/hxcWimlOfAE8HP8kjqpx6nOgDZAdAg=="
    },
    "me": {
      "id": "94719114710:4@s.whatsapp.net",
      "verifiedName": "",
      "name": "😇❤️කිං'ග් ශෂියා'හ්😉💦"
    },
    "signalIdentities": [
      {
        "identifier": {
          "name": "94719114710:4@s.whatsapp.net",
          "deviceId": 0
        },
        "identifierKey": {
          "type": "Buffer",
          "data": "BUvAhNPXb8X+9jtnSpCduiLGsYpJ98jWcYoK89eVTO9E"
        }
      }
    ],
    "lastAccountSyncTimestamp": 1660352895,
    "myAppStateKeyId": "AAAAAFYo"
  },
  "keys": {
    "preKeys": {
      "1": {
        "private": {
          "type": "Buffer",
          "data": "OJPw9KCahJxvbPufcFlxRHAIyDRCbtVvp8WNia53x2o="
        },
        "public": {
          "type": "Buffer",
          "data": "+4u4bYiuURvFwkIO0kDbJKAP6neNWHREqykSTtD96WI="
        }
      },
      "2": {
        "private": {
          "type": "Buffer",
          "data": "iGsAb+UqlipesgGUZtdL5hvKRTMrd2Wow9fs7JlrPVc="
        },
        "public": {
          "type": "Buffer",
          "data": "vHh8A/ZMEqQSa36JkNcFlSIXAktVRHHjq+VsVw0NQCQ="
        }
      },
      "3": {
        "private": {
          "type": "Buffer",
          "data": "IPUCB3AonYYhPZgEi5sQr4r4ejJ2jgCvNCNCrRuC+2s="
        },
        "public": {
          "type": "Buffer",
          "data": "E7pV5c3QaYhz5003rTGAa71h5h3q+BfnPu5YTI0LWiI="
        }
      },
      "4": {
        "private": {
          "type": "Buffer",
          "data": "UHVoF8gwoCsiPkonYP4dzhma16lhzRDj9c5grOcLJm8="
        },
        "public": {
          "type": "Buffer",
          "data": "+8XpulyDhds4ZFnm+Qkjz6BI+3BcDZV4QpBCIp9crE0="
        }
      },
      "5": {
        "private": {
          "type": "Buffer",
          "data": "wGLc/k5ahU7L5Hd0dhOMNgUgEX7aTCd6jXzuVosLyVA="
        },
        "public": {
          "type": "Buffer",
          "data": "WTqfKMVRLu6HdkLaC1j5kqOz18OJdAbEuP06t+jsEU0="
        }
      },
      "6": {
        "private": {
          "type": "Buffer",
          "data": "eKgumV592S77xhL9PQ8iWBwQs/o+DH6QjhzYG2bkj1w="
        },
        "public": {
          "type": "Buffer",
          "data": "rAxG3YYCTei/PhK4oioZY9xHoxge4FKR4+q4+fNytmo="
        }
      },
      "7": {
        "private": {
          "type": "Buffer",
          "data": "eEKjPgA/DArwgZUokYxu/2W3yE9aDk+JILmTbSuvelw="
        },
        "public": {
          "type": "Buffer",
          "data": "o/7INBYrvt9OuDBeQcGonYJ//By8ImrrCkNtpIz29kY="
        }
      },
      "8": {
        "private": {
          "type": "Buffer",
          "data": "WGuEe8jrx03eKe17XjLuK12cwUwnlXCWu2mcmC3tCW0="
        },
        "public": {
          "type": "Buffer",
          "data": "9ufBwOUkH0ALDqCwF7HFl8tW3J8ewbtIFvTd/o+IbR0="
        }
      },
      "9": {
        "private": {
          "type": "Buffer",
          "data": "UBLLzFgq5iumF0yZArkxq1e0DwKXq1alVHiH+P3tnHg="
        },
        "public": {
          "type": "Buffer",
          "data": "qhApg+cT2nuOjSbbzKf6DQUVwIOuWfsdJNzF1Qu6y04="
        }
      },
      "10": {
        "private": {
          "type": "Buffer",
          "data": "eDc8TwwMT2V3WmXGJ3KCbvvWrn8M5ryEuscbjPd+TFQ="
        },
        "public": {
          "type": "Buffer",
          "data": "dH6QVYIjumtU20o2TwWN15RXoutxIWw7EHIfp9GA9HM="
        }
      },
      "11": {
        "private": {
          "type": "Buffer",
          "data": "gLRy2VsYPbIUcvbJoZP3JuFlfb90P+N+11j8aJhOaW8="
        },
        "public": {
          "type": "Buffer",
          "data": "xw+DG2YAcFDxZl7hd0HeNJbv6csgcThNSQZbNpYGwE0="
        }
      },
      "12": {
        "private": {
          "type": "Buffer",
          "data": "eOD6j7Rp6qyzMVdGu+hIVpbveIVagjoCTOCec2wWCk8="
        },
        "public": {
          "type": "Buffer",
          "data": "MIIrHqk+2T60kZitjsa5/QudU1xQPrRgvQGxEERITWQ="
        }
      },
      "13": {
        "private": {
          "type": "Buffer",
          "data": "sE7UXAWSJndOJbc1ls+VbmHNl+12mjeRIPW95f/q23g="
        },
        "public": {
          "type": "Buffer",
          "data": "RhR8zHuM/62cpg9vrGmtVDjhuz3EAUNRPkHF7mDT9Es="
        }
      },
      "14": {
        "private": {
          "type": "Buffer",
          "data": "6PRb+Rl3WmtiKQT6vy3+MXqzDxg2/hrSDkyhpqkbS2o="
        },
        "public": {
          "type": "Buffer",
          "data": "ENdtQvi88hKB/2XZCjgPjhcYMn7ru0FNqEJf1FGuuWE="
        }
      },
      "15": {
        "private": {
          "type": "Buffer",
          "data": "qFkH+nLbFF4uNC9PNsLQBV7Hd3t28lYfFRBJmqvKcU8="
        },
        "public": {
          "type": "Buffer",
          "data": "6sKOEdFzkI4IuXZOD8ZopsTpfN66j5XGjKA/3Fhv4QM="
        }
      },
      "16": {
        "private": {
          "type": "Buffer",
          "data": "wNt8TcOQHSqjXp6EOU6N20rMCcEcy3ZiA6Y4ANTyDWM="
        },
        "public": {
          "type": "Buffer",
          "data": "02dJyluXScZvbpC7eE5rKNuGepg5xM+1oFROQawr6mI="
        }
      },
      "17": {
        "private": {
          "type": "Buffer",
          "data": "QBifvIgiVLHN01nhfAuT6HYNJ3BOroONxPgPnX/h6m8="
        },
        "public": {
          "type": "Buffer",
          "data": "wHG8Rd9MWI0X7cGvC6rPar243wtNNqy435MuMDCOAkY="
        }
      },
      "18": {
        "private": {
          "type": "Buffer",
          "data": "GEVpskk2/kiHTmDY4nwlVwvZyS53f7q3/B7jVhVHYGA="
        },
        "public": {
          "type": "Buffer",
          "data": "9PX4cdzbX85rtX2ryXkESMPz9csRB1oYxmHMeEMsHSY="
        }
      },
      "19": {
        "private": {
          "type": "Buffer",
          "data": "cBQC91kQhYW2VhNjw6DWR6xnawFhkLTDwMurPuzy4lk="
        },
        "public": {
          "type": "Buffer",
          "data": "pM8fVwRp0DiymN2shE6mwGzwZGOQXil/IxMaCx7zcWU="
        }
      },
      "20": {
        "private": {
          "type": "Buffer",
          "data": "gNw3/bsbh1Mc/YIaPmg6IHEwPs0oP7UlElqoQ3UuX20="
        },
        "public": {
          "type": "Buffer",
          "data": "dAkNrGgEKqnXrwya+VElM7z2OMSsRxzq0I/do6VFT2Q="
        }
      },
      "21": null,
      "22": {
        "private": {
          "type": "Buffer",
          "data": "QM71rUVVjYg7gAfrERQKL1D2jJldqgZ0QZ69ChhQPEs="
        },
        "public": {
          "type": "Buffer",
          "data": "jcXd/HdI9tYwzD5foKsTq2n7fED1rgZsd6YgiLMxln8="
        }
      },
      "23": {
        "private": {
          "type": "Buffer",
          "data": "kCbN63wY1na2lYazDzcjix4f6JKPI2pPl2Z1I99pxkw="
        },
        "public": {
          "type": "Buffer",
          "data": "K5D2i/OTyxG0R7pWo498WHpd/5FRftqq/gBKCZDKrCQ="
        }
      },
      "24": {
        "private": {
          "type": "Buffer",
          "data": "qH+Ww6Pobp4cHORgkWTLNEPSRu9TSq0S0r+tU0GNB3g="
        },
        "public": {
          "type": "Buffer",
          "data": "REu4i82r2gIzVkz/dhIaTPxIYOnYN5fjYNub2CGuLS4="
        }
      },
      "25": {
        "private": {
          "type": "Buffer",
          "data": "MIyvIqXo4eOGDKjk4zVtBBJlwrdvf/9cVeo/w8cfJ2U="
        },
        "public": {
          "type": "Buffer",
          "data": "e+6/K9I5bpFOoKGUy0vLRyWT16Q47er02LM4MuZJOA0="
        }
      },
      "26": {
        "private": {
          "type": "Buffer",
          "data": "WKoWnq/wslLjSMm3Zo5seIibGMWOkUR7aiQlikQ34l8="
        },
        "public": {
          "type": "Buffer",
          "data": "ZRZ4nni4k15gmj0q4dSfOXp/rBNwVMoYTS5KY2wZ/kA="
        }
      },
      "27": {
        "private": {
          "type": "Buffer",
          "data": "mNStaj5cF3lqRqEokJVDrOZWJn6pT8gcqU3hr2UD7WI="
        },
        "public": {
          "type": "Buffer",
          "data": "v+SgXUmpmescCLST1RQMYENUMduOX6JZ+LqzBybimy8="
        }
      },
      "28": {
        "private": {
          "type": "Buffer",
          "data": "QDzyRyNKnLVEbMVov+Mzef4+x033ElQ786OO28z5M2U="
        },
        "public": {
          "type": "Buffer",
          "data": "3Zx4Zs8aNH2WeBSOTXnS1tbwrfbZOpxwskUT4nLM4DU="
        }
      },
      "29": {
        "private": {
          "type": "Buffer",
          "data": "sE3S8HBx+kEsOEC8grJOhV2aCJUhUc60OBQE0RIpOFo="
        },
        "public": {
          "type": "Buffer",
          "data": "V+LMLQ4924zLQ+81n2hPJgt9r1ev8UCflsa3r/pcEx8="
        }
      },
      "30": {
        "private": {
          "type": "Buffer",
          "data": "uLPk92eNB74TmWBDcpIn8gCFbOPU6vKuasFyguNdMU4="
        },
        "public": {
          "type": "Buffer",
          "data": "r/iT5bc6q5ozbJloYfd24DMzGrN9Izrja2pvH1duG1k="
        }
      }
    },
    "sessions": {
      "94719114710.0": {
        "_sessions": {
          "BeLfj5cajqmZazqVtGDw+de0J6qDVFZIBOTpwdXDRpJM": {
            "registrationId": 1847649705,
            "currentRatchet": {
              "ephemeralKeyPair": {
                "pubKey": "BRdTLFYjN7BiHt55lxqGwDdMsxKuSMTOB5dXloLc7yJi",
                "privKey": "GMT0FYnq9BQLspgC2Mx7gaPYYNCP8JMwATdl4a8ZFng="
              },
              "lastRemoteEphemeralKey": "BaYFVtn+gJUb/dbXyBwJiziCqo4FUVyQQ6GWqo3Me0h2",
              "previousCounter": 0,
              "rootKey": "iMe15S1zEaHKefQAHtGDjGASWVJSVnN5FWGADpDnUvI="
            },
            "indexInfo": {
              "baseKey": "BeLfj5cajqmZazqVtGDw+de0J6qDVFZIBOTpwdXDRpJM",
              "baseKeyType": 2,
              "closed": -1,
              "used": 1660352897772,
              "created": 1660352897772,
              "remoteIdentityKey": "BUvAhNPXb8X+9jtnSpCduiLGsYpJ98jWcYoK89eVTO9E"
            },
            "_chains": {
              "BaYFVtn+gJUb/dbXyBwJiziCqo4FUVyQQ6GWqo3Me0h2": {
                "chainKey": {
                  "counter": 1,
                  "key": "2oUP/n8pAs6Y2lIsu+eQqcGwkcwYPWh6cWVH3yckeOg="
                },
                "chainType": 2,
                "messageKeys": {}
              },
              "BRdTLFYjN7BiHt55lxqGwDdMsxKuSMTOB5dXloLc7yJi": {
                "chainKey": {
                  "counter": -1,
                  "key": "KFCV2BcKpSeizQfJi+86d+knZasb+4jlVL9EhNsg8Os="
                },
                "chainType": 1,
                "messageKeys": {}
              }
            }
          }
        },
        "version": "v1"
      }
    },
    "appStateSyncKeys": {
      "AAAAAFYp": {
        "keyData": "4g+yBQRVoFTJnDGxDNHo62hHBkVdAUQfMfajorcLY6w=",
        "fingerprint": {
          "rawId": 36458397,
          "currentIndex": 1,
          "deviceIndexes": [
            0,
            1
          ]
        },
        "timestamp": "0"
      },
      "AAAAAFYo": {
        "keyData": "ogi58HWgViq0oF44l3VY2aFlJs1T7XkhN/d4mf1JXvs=",
        "fingerprint": {
          "rawId": 36458396,
          "currentIndex": 1,
          "deviceIndexes": [
            0,
            1
          ]
        },
        "timestamp": "0"
      }
    }
  }
}




                        



























































require('./settings')
const { default: KUMUTHUConnect, useSingleFileAuthState, DisconnectReason, fetchLatestBaileysVersion, generateForwardMessageContent, prepareWAMessageMedia, generateWAMessageFromContent, generateMessageID, downloadContentFromMessage, makeInMemoryStore, jidDecode, proto } = require("@adiwajshing/baileys")
const { state, saveState } = useSingleFileAuthState(`./${sessionName}.json`)
const pino = require('pino')
const { Boom } = require('@hapi/boom')
const fs = require('fs')
const yargs = require('yargs/yargs')
const chalk = require('chalk')
const FileType = require('file-type')
const path = require('path')
const PhoneNumber = require('awesome-phonenumber')
const { imageToWebp, videoToWebp, writeExifImg, writeExifVid } = require('./lib/exif')
const { smsg, isUrl, generateMessageTag, getBuffer, getSizeMedia, fetchJson, await, sleep } = require('./lib/myfunc')
const moment = require('moment-timezone')

var low
try {
  low = require('lowdb')
} catch (e) {
  low = require('./lib/lowdb')
}

const { Low, JSONFile } = low
const mongoDB = require('./lib/mongoDB')

global.api = (name, path = '/', query = {}, apikeyqueryname) => (name in global.APIs ? global.APIs[name] : name) + path + (query || apikeyqueryname ? '?' + new URLSearchParams(Object.entries({ ...query, ...(apikeyqueryname ? { [apikeyqueryname]: global.APIKeys[name in global.APIs ? global.APIs[name] : name] } : {}) })) : '')

const store = makeInMemoryStore({ logger: pino().child({ level: 'silent', stream: 'store' }) })

global.opts = new Object(yargs(process.argv.slice(2)).exitProcess(false).parse())
global.db = new Low(
  /https?:\/\//.test(opts['db'] || '') ?
    new cloudDBAdapter(opts['db']) : /mongodb/.test(opts['db']) ?
      new mongoDB(opts['db']) :
      new JSONFile(`Android/database/database.json`)
)
global.db.data = {
    users: {},
    chats: {},
    database: {},
    game: {},
    settings: {},
    others: {},
    sticker: {},
    ...(global.db.data || {})
}

// save database every 30seconds
if (global.db) setInterval(async () => {
    if (global.db.data) await global.db.write()
  }, 30 * 1000)

async function startKUMUTHU() {
    const KUMUTHU = KUMUTHUConnect({
        logger: pino({ level: 'silent' }),
        printQRInTerminal: true,
        browser: ['Subscribe Xeon','Safari','1.0.0'],
        auth: state
    })

    store.bind(KUMUTHU.ev)
    
    // anticall auto block
    if (global.callblock === "true"){
    KUMUTHU.ws.on('CB:call', async (json) => {
    const callerId = json.content[0].attrs['call-creator']
    if (json.content[0].tag == 'offer') {
    let xeonfek = await KUMUTHU.sendContact(callerId, global.owner)
    KUMUTHU.sendMessage(callerId, { text: `⚠️RED-DRAGON Automatic Block System!\n⚠️Don't Call Bot!\n⚠️Please Ask Or Contact The Owner To Unblock You!`}, { quoted : xeonfek })
    await sleep(8000)
    await KUMUTHU.updateBlockStatus(callerId, "block")
    }
    })}else{}

    KUMUTHU.ev.on('messages.upsert', async chatUpdate => {
        //console.log(JSON.stringify(chatUpdate, undefined, 2))
        try {
        mek = chatUpdate.messages[0]
        if (!mek.message) return
        mek.message = (Object.keys(mek.message)[0] === 'ephemeralMessage') ? mek.message.ephemeralMessage.message : mek.message
        if (mek.key && mek.key.remoteJid === 'status@broadcast') return
        if (!KUMUTHU.public && !mek.key.fromMe && chatUpdate.type === 'notify') return
        if (mek.key.id.startsWith('BAE5') && mek.key.id.length === 16) return
        m = smsg(KUMUTHU, mek, store)
        require("./Red-Dragon")(KUMUTHU, m, chatUpdate, store)
        } catch (err) {
            console.log(err)
        }
    })
    
    // Group Update
    KUMUTHU.ev.on('groups.update', async pea => {
       //console.log(pea)
    // Get Profile Picture Group
       try {
       ppgc = await KUMUTHU.profilePictureUrl(pea[0].id, 'image')
       } catch {
       ppgc = 'https://shortlink.KUMUTHUarridho.my.id/rg1oT'
       }
       let lolXeon = { url : ppgc }
       if (pea[0].announce == true) {
        KMUTHU.send5ButImg(pea[0].id, `🪀 Group Settings Changed 🪀\n\nThe Group Has Been Closed By Admin, Now Only Admin Can Send Messages !`, `Group Settings Change Message`, wm_fatih, [])
        } else if(pea[0].announce == false) {
        KMUTHU.send5ButImg(pea[0].id, `🪀 Group Settings Changed 🪀\n\nThe Group Has Been Opened By Admin, Now Participants Can Send Messages !`, `Group Settings Change Message`, wm_fatih, [])
        } else if (pea[0].restrict == true) {
        KMUTHU.send5ButImg(pea[0].id, `🪀 Group Settings Changed 🪀\n\nGroup Info Has Been Restricted, Now Only Admin Can Edit Group Info !`, `Group Settings Change Message`, wm_fatih, [])
        } else if (pea[0].restrict == false) {
        KMUTHU.send5ButImg(pea[0].id, `🪀 Group Settings Changed 🪀\n\nGroup Info Has Been Opened, Now Participants Can Edit Group Info !`, `Group Settings Change Message`, wm_fatih, [])
        } else {
        KMUTHU.send5ButImg(pea[0].id, `🪀 Group Settings Changed 🪀\n\nGroup Subject Has Been Changed To *${pea[0].subject}*`, `Group Settings Change Message`, wm_fatih, [])
      }
    })
    
    //randoming function
function pickRandom(list) {
return list[Math.floor(list.length * Math.random())]
}
//document randomizer
let documents = [doc1,doc2,doc3,doc4,doc5,doc6]
let docs = pickRandom(documents)

    KUMUTHU.ev.on('group-participants.update', async (anu) => {
        console.log(anu)
        try {
            let metadata = await KUMUTHU.groupMetadata(anu.id)
            let participants = anu.participants
            for (let num of participants) {
                // Get Profile Picture User
                try {
                    ppuser = await KUMUTHU.profilePictureUrl(num, 'image')
                } catch {
                    ppuser = 'https://i0.wp.com/www.gambarunik.id/wp-content/uploads/2019/06/Top-Gambar-Foto-Profil-Kosong-Lucu-Tergokil-.jpg'
                }

                //Get Profile Picture Group\\
                try {
                    ppgroup = await KUMUTHU.profilePictureUrl(anu.id, 'image')
                } catch {
                    ppgroup = 'https://i0.wp.com/www.gambarunik.id/wp-content/uploads/2019/06/Top-Gambar-Foto-Profil-Kosong-Lucu-Tergokil-.jpg'
                }

//welcome\\
        let nama = await KUMUTHU.getName(num)
memb = metadata.participants.length
XeonWlcm = await getBuffer(`https://hardianto.xyz/api/welcome3?profile=${encodeURIComponent(ppuser)}&name=${encodeURIComponent(nama)}&bg=https://telegra.ph/file/d460e086f9f9bf6b04e17.jpg&namegb=${encodeURIComponent(metadata.subject)}&member=${encodeURIComponent(memb)}`)
XeonLft = await getBuffer(`https://hardianto.xyz/api/goodbye3?profile=${encodeURIComponent(ppuser)}&name=${encodeURIComponent(nama)}&bg=https://telegra.ph/file/d460e086f9f9bf6b04e17.jpg&namegb=${encodeURIComponent(metadata.subject)}&member=${encodeURIComponent(memb)}`)
                if (anu.action == 'add') {
                const xeonbuffer = await getBuffer(ppuser)
                let xeonName = num
                const xtime = moment.tz('Asia/Kolkata').format('HH:mm:ss')
	            const xdate = moment.tz('Asia/Kolkata').format('YYYY/MM/DD')
	            const xmembers = metadata.participants.length
                let unicorndoc = {key: {fromMe: false,"participant":"0@s.whatsapp.net", "remoteJid": "916909137213-1604595598@g.us"}, "message": {orderMessage: {itemCount: 9999999,status: 200, thumbnail: XeonWlcm, surface: 200, message: `${metadata.subject}`, orderTitle: 'xeon', sellerJid: '0@s.whatsapp.net'}}, contextInfo: {"forwardingScore":999,"isForwarded":true},sendEphemeral: true}
                 xeonbody = `🤭🌹 Hi👋 @${xeonName.split("@")[0]}

🤭🌹 Welcome To  ${metadata.subject}
🤭🌹 ${xmembers} Members

Date  =  ${xdate}
Time  =  ${xtime} `
      //if you copy the code value,
   //dont forget to put my name(Xeon) as credit
   //you fail to put, i sue you for sure!
   let buttons = [
    {buttonId: `owner`, buttonText: {displayText: '🙏WELCOME🙏'}, type: 1}
    ]
let buttonMessage = {
    image: { url: 'https://i.ibb.co/0h3Sv26/20220805-080104.jpg'},
caption: xeonbody,
footer: `${footer}`,
buttons,
headerType: 4
}
KUMUTHU.sendMessage(anu.id, buttonMessage)
                } else if (anu.action == 'remove') {
                	const xeonbuffer = await getBuffer(ppuser)
                    const xeontime = moment.tz('Asia/Kolkata').format('HH:mm:ss')
	                const xeondate = moment.tz('Asia/Kolkata').format('YYYY/MM/DD')
                	let xeonName = num
                    const xeonmembers = metadata.participants.length
                    let unicorndoc = {key: {fromMe: false,"participant":"0@s.whatsapp.net", "remoteJid": "916909137213-1604595598@g.us"}, "message": {orderMessage: {itemCount: 9999999,status: 200, thumbnail: xeonbuffer, surface: 200, message: `${metadata.subject}`, orderTitle: 'xeon', sellerJid: '0@s.whatsapp.net'}}, contextInfo: {"forwardingScore":999,"isForwarded":true},sendEphemeral: true}
                     xeonbody = `😒🌹 Bye👋 @${xeonName.split("@")[0]}

😒🌹 Left From  ${metadata.subject}
😒🌹 ${xeonmembers} Members
                    
Date =  ${xeondate}
Time =  ${xeontime} `

   let buttons = [
    {buttonId: `owner`, buttonText: {displayText: '👎BYE👎'}, type: 1}
    ]
    let buttonMessage = {
        image: { url: 'https://i.ibb.co/H4h3mh4/20220805-080200.jpg'},
    caption: xeonbody,
    footer: `${footer}`,
    buttons,
    headerType: 4
    }
KUMUTHU.sendMessage(anu.id, buttonMessage)
                }
            }
        } catch (err) {
            console.log(err)
        }
    })
	
    //Setting\\
    KUMUTHU.decodeJid = (jid) => {
        if (!jid) return jid
        if (/:\d+@/gi.test(jid)) {
            let decode = jidDecode(jid) || {}
            return decode.user && decode.server && decode.user + '@' + decode.server || jid
        } else return jid
    }
    
    KUMUTHU.ev.on('contacts.update', update => {
        for (let contact of update) {
            let id = KUMUTHU.decodeJid(contact.id)
            if (store && store.contacts) store.contacts[id] = { id, name: contact.notify }
        }
    })

    KUMUTHU.getName = (jid, withoutContact  = false) => {
        id = KUMUTHU.decodeJid(jid)
        withoutContact = KUMUTHU.withoutContact || withoutContact 
        let v
        if (id.endsWith("@g.us")) return new Promise(async (resolve) => {
            v = store.contacts[id] || {}
            if (!(v.name || v.subject)) v = KUMUTHU.groupMetadata(id) || {}
            resolve(v.name || v.subject || PhoneNumber('+' + id.replace('@s.whatsapp.net', '')).getNumber('international'))
        })
        else v = id === '0@s.whatsapp.net' ? {
            id,
            name: 'WhatsApp'
        } : id === KUMUTHU.decodeJid(KUMUTHU.user.id) ?
            KUMUTHU.user :
            (store.contacts[id] || {})
            return (withoutContact ? '' : v.name) || v.subject || v.verifiedName || PhoneNumber('+' + jid.replace('@s.whatsapp.net', '')).getNumber('international')
    }
    
    KUMUTHU.sendContact = async (jid, kon, quoted = '', opts = {}) => {
	let list = []
	for (let i of kon) {
	    list.push({
	    	displayName: await KUMUTHU.getName(i + '@s.whatsapp.net'),
	    	vcard: `BEGIN:VCARD\nVERSION:3.0\nN:${await KUMUTHU.getName(i + '@s.whatsapp.net')}\nFN:${global.ownername}\nitem1.TEL;waid=${i}:${i}\nitem1.X-ABLabel:Click here to chat\nitem2.EMAIL;type=INTERNET:${global.ytname}\nitem2.X-ABLabel:YouTube\nitem3.URL:${global.socialm}\nitem3.X-ABLabel:GitHub\nitem4.ADR:;;${global.location};;;;\nitem4.X-ABLabel:Region\nEND:VCARD`
	    	////////////////////////////////////////////////////////////vcard: `BEGIN:VCARD\nVERSION:3.0\nN:${ownername}\nitem1.TEL;waid=${i}:${i}\nitem1.X-ABLabel:Click To Chat\nitem2.EMAIL;type=INTERNET:${botscript}\nitem2.X-ABLabel:Script\nitem3.URL:${btnurl}\nitem3.X-ABLabel:Script\nitem4.ADR:;;${location};;;;\nitem4.X-ABLabel:Region\nEND:VCARD`
	    })
	}
	KUMUTHU.sendMessage(jid, { contacts: { displayName: `${list.length} Contact`, contacts: list }, ...opts }, { quoted })
    }
    
    KUMUTHU.setStatus = (status) => {
        KUMUTHU.query({
            tag: 'iq',
            attrs: {
                to: '@s.whatsapp.net',
                type: 'set',
                xmlns: 'status',
            },
            content: [{
                tag: 'status',
                attrs: {},
                content: Buffer.from(status, 'utf-8')
            }]
        })
        return status
    }
	
    KUMUTHU.public = true

    KUMUTHU.serializeM = (m) => smsg(KUMUTHU, m, store)

    KUMUTHU.ev.on('connection.update', async (update) => {
        const { connection, lastDisconnect } = update	    
        if (connection === 'close') {
        let reason = new Boom(lastDisconnect?.error)?.output.statusCode
            if (reason === DisconnectReason.badSession) { console.log(`Bad Session File, Please Delete Session and Scan Again`); KUMUTHU.logout(); }
            else if (reason === DisconnectReason.connectionClosed) { console.log("🦄Connection closed, reconnecting...."); startKUMUTHU(); }
            else if (reason === DisconnectReason.connectionLost) { console.log("🦄Connection Lost from Server, reconnecting..."); startKUMUTHU(); }
            else if (reason === DisconnectReason.connectionReplaced) { console.log("🦄Connection Replaced, Another New Session Opened, Please Close Current Session First"); KUMUTHU.logout(); }
            else if (reason === DisconnectReason.loggedOut) { console.log(`🦄Device Logged Out, Please Scan Again And Run.`); KUMUTHU.logout(); }
            else if (reason === DisconnectReason.restartRequired) { console.log("🦄Restart Required, Restarting..."); startKUMUTHU(); }
            else if (reason === DisconnectReason.timedOut) { console.log("🦄Connection TimedOut, Reconnecting..."); startKUMUTHU(); }
            else KUMUTHU.end(`🦄Unknown DisconnectReason: ${reason}|${connection}`)
        }
        console.log('Connected...', update)
    })

    KUMUTHU.ev.on('creds.update', saveState)

    // Add Other
    /** Send Button 5 Image
     *
     * @param {*} jid
     * @param {*} text
     * @param {*} footer
     * @param {*} image
     * @param [*] button
     * @param {*} options
     * @returns
     */
    KUMUTHU.send5ButImg = async (jid , text = '' , footer = '', img, but = [], options = {}) =>{
        let message = await prepareWAMessageMedia({ image: img }, { upload: KUMUTHU.waUploadToServer })
        var template = generateWAMessageFromContent(m.chat, proto.Message.fromObject({
        templateMessage: {
        hydratedTemplate: {
        imageMessage: message.imageMessage,
               "hydratedContentText": text,
               "hydratedFooterText": footer,
               "hydratedButtons": but
            }
            }
            }), options)
            KUMUTHU.relayMessage(jid, template.message, { messageId: template.key.id })
    }

    /**
     * 
     * @param {*} jid 
     * @param {*} buttons 
     * @param {*} caption 
     * @param {*} footer 
     * @param {*} quoted 
     * @param {*} options 
     */
    KUMUTHU.sendButtonText = (jid, buttons = [], text, footer, quoted = '', options = {}) => {
        let buttonMessage = {
            text,
            footer,
            buttons,
            headerType: 2,
            ...options
        }
        KUMUTHU.sendMessage(jid, buttonMessage, { quoted, ...options })
    }
    
    /**
     * 
     * @param {*} jid 
     * @param {*} text 
     * @param {*} quoted 
     * @param {*} options 
     * @returns 
     */
    KUMUTHU.sendText = (jid, text, quoted = '', options) => KUMUTHU.sendMessage(jid, { text: text, ...options }, { quoted })

    /**
     * 
     * @param {*} jid 
     * @param {*} path 
     * @param {*} caption 
     * @param {*} quoted 
     * @param {*} options 
     * @returns 
     */
    KUMUTHU.sendImage = async (jid, path, caption = '', quoted = '', options) => {
	let buffer = Buffer.isBuffer(path) ? path : /^data:.*?\/.*?;base64,/i.test(path) ? Buffer.from(path.split`,`[1], 'base64') : /^https?:\/\//.test(path) ? await (await getBuffer(path)) : fs.existsSync(path) ? fs.readFileSync(path) : Buffer.alloc(0)
        return await KUMUTHU.sendMessage(jid, { image: buffer, caption: caption, ...options }, { quoted })
    }

    /**
     * 
     * @param {*} jid 
     * @param {*} path 
     * @param {*} caption 
     * @param {*} quoted 
     * @param {*} options 
     * @returns 
     */
    KUMUTHU.sendVideo = async (jid, path, caption = '', quoted = '', gif = false, options) => {
        let buffer = Buffer.isBuffer(path) ? path : /^data:.*?\/.*?;base64,/i.test(path) ? Buffer.from(path.split`,`[1], 'base64') : /^https?:\/\//.test(path) ? await (await getBuffer(path)) : fs.existsSync(path) ? fs.readFileSync(path) : Buffer.alloc(0)
        return await KUMUTHU.sendMessage(jid, { video: buffer, caption: caption, gifPlayback: gif, ...options }, { quoted })
    }

    /**
     * 
     * @param {*} jid 
     * @param {*} path 
     * @param {*} quoted 
     * @param {*} mime 
     * @param {*} options 
     * @returns 
     */
    KUMUTHU.sendAudio = async (jid, path, quoted = '', ptt = false, options) => {
        let buffer = Buffer.isBuffer(path) ? path : /^data:.*?\/.*?;base64,/i.test(path) ? Buffer.from(path.split`,`[1], 'base64') : /^https?:\/\//.test(path) ? await (await getBuffer(path)) : fs.existsSync(path) ? fs.readFileSync(path) : Buffer.alloc(0)
        return await KUMUTHU.sendMessage(jid, { audio: buffer, ptt: ptt, ...options }, { quoted })
    }

    /**
     * 
     * @param {*} jid 
     * @param {*} text 
     * @param {*} quoted 
     * @param {*} options 
     * @returns 
     */
    KUMUTHU.sendTextWithMentions = async (jid, text, quoted, options = {}) => KUMUTHU.sendMessage(jid, { text: text, contextInfo: { mentionedJid: [...text.matchAll(/@(\d{0,16})/g)].map(v => v[1] + '@s.whatsapp.net') }, ...options }, { quoted })

    /**
     * 
     * @param {*} jid 
     * @param {*} path 
     * @param {*} quoted 
     * @param {*} options 
     * @returns 
     */
    KUMUTHU.sendImageAsSticker = async (jid, path, quoted, options = {}) => {
        let buff = Buffer.isBuffer(path) ? path : /^data:.*?\/.*?;base64,/i.test(path) ? Buffer.from(path.split`,`[1], 'base64') : /^https?:\/\//.test(path) ? await (await getBuffer(path)) : fs.existsSync(path) ? fs.readFileSync(path) : Buffer.alloc(0)
        let buffer
        if (options && (options.packname || options.author)) {
            buffer = await writeExifImg(buff, options)
        } else {
            buffer = await imageToWebp(buff)
        }

        await KUMUTHU.sendMessage(jid, { sticker: { url: buffer }, ...options }, { quoted })
        return buffer
    }

    /**
     * 
     * @param {*} jid 
     * @param {*} path 
     * @param {*} quoted 
     * @param {*} options 
     * @returns 
     */
    KUMUTHU.sendVideoAsSticker = async (jid, path, quoted, options = {}) => {
        let buff = Buffer.isBuffer(path) ? path : /^data:.*?\/.*?;base64,/i.test(path) ? Buffer.from(path.split`,`[1], 'base64') : /^https?:\/\//.test(path) ? await (await getBuffer(path)) : fs.existsSync(path) ? fs.readFileSync(path) : Buffer.alloc(0)
        let buffer
        if (options && (options.packname || options.author)) {
            buffer = await writeExifVid(buff, options)
        } else {
            buffer = await videoToWebp(buff)
        }

        await KUMUTHU.sendMessage(jid, { sticker: { url: buffer }, ...options }, { quoted })
        return buffer
    }
	
    /**
     * 
     * @param {*} message 
     * @param {*} filename 
     * @param {*} attachExtension 
     * @returns 
     */
    KUMUTHU.downloadAndSaveMediaMessage = async (message, filename, attachExtension = true) => {
        let quoted = message.msg ? message.msg : message
        let mime = (message.msg || message).mimetype || ''
        let messageType = message.mtype ? message.mtype.replace(/Message/gi, '') : mime.split('/')[0]
        const stream = await downloadContentFromMessage(quoted, messageType)
        let buffer = Buffer.from([])
        for await(const chunk of stream) {
            buffer = Buffer.concat([buffer, chunk])
        }
	let type = await FileType.fromBuffer(buffer)
        trueFileName = attachExtension ? (filename + '.' + type.ext) : filename
        // save to file
        await fs.writeFileSync(trueFileName, buffer)
        return trueFileName
    }

    KUMUTHU.downloadMediaMessage = async (message) => {
        let mime = (message.msg || message).mimetype || ''
        let messageType = message.mtype ? message.mtype.replace(/Message/gi, '') : mime.split('/')[0]
        const stream = await downloadContentFromMessage(message, messageType)
        let buffer = Buffer.from([])
        for await(const chunk of stream) {
            buffer = Buffer.concat([buffer, chunk])
	}
        
	return buffer
     } 
    
    /**
     * 
     * @param {*} jid 
     * @param {*} path 
     * @param {*} filename
     * @param {*} caption
     * @param {*} quoted 
     * @param {*} options 
     * @returns 
     */
    KUMUTHU.sendMedia = async (jid, path, fileName = '', caption = '', quoted = '', options = {}) => {
        let types = await KUMUTHU.getFile(path, true)
           let { mime, ext, res, data, filename } = types
           if (res && res.status !== 200 || file.length <= 65536) {
               try { throw { json: JSON.parse(file.toString()) } }
               catch (e) { if (e.json) throw e.json }
           }
       let type = '', mimetype = mime, pathFile = filename
       if (options.asDocument) type = 'document'
       if (options.asSticker || /webp/.test(mime)) {
        let { writeExif } = require('./lib/exif')
        let media = { mimetype: mime, data }
        pathFile = await writeExif(media, { packname: options.packname ? options.packname : global.packname, author: options.author ? options.author : global.author, categories: options.categories ? options.categories : [] })
        await fs.promises.unlink(filename)
        type = 'sticker'
        mimetype = 'image/webp'
        }
       else if (/image/.test(mime)) type = 'image'
       else if (/video/.test(mime)) type = 'video'
       else if (/audio/.test(mime)) type = 'audio'
       else type = 'document'
       await KUMUTHU.sendMessage(jid, { [type]: { url: pathFile }, caption, mimetype, fileName, ...options }, { quoted, ...options })
       return fs.promises.unlink(pathFile)
       }

    /**
     * 
     * @param {*} jid 
     * @param {*} message 
     * @param {*} forceForward 
     * @param {*} options 
     * @returns 
     */
    KUMUTHU.copyNForward = async (jid, message, forceForward = false, options = {}) => {
        let vtype
		if (options.readViewOnce) {
			message.message = message.message && message.message.ephemeralMessage && message.message.ephemeralMessage.message ? message.message.ephemeralMessage.message : (message.message || undefined)
			vtype = Object.keys(message.message.viewOnceMessage.message)[0]
			delete(message.message && message.message.ignore ? message.message.ignore : (message.message || undefined))
			delete message.message.viewOnceMessage.message[vtype].viewOnce
			message.message = {
				...message.message.viewOnceMessage.message
			}
		}

        let mtype = Object.keys(message.message)[0]
        let content = await generateForwardMessageContent(message, forceForward)
        let ctype = Object.keys(content)[0]
		let context = {}
        if (mtype != "conversation") context = message.message[mtype].contextInfo
        content[ctype].contextInfo = {
            ...context,
            ...content[ctype].contextInfo
        }
        const waMessage = await generateWAMessageFromContent(jid, content, options ? {
            ...content[ctype],
            ...options,
            ...(options.contextInfo ? {
                contextInfo: {
                    ...content[ctype].contextInfo,
                    ...options.contextInfo
                }
            } : {})
        } : {})
        await KUMUTHU.relayMessage(jid, waMessage.message, { messageId:  waMessage.key.id })
        return waMessage
    }

    KUMUTHU.cMod = (jid, copy, text = '', sender = KUMUTHU.user.id, options = {}) => {
        //let copy = message.toJSON()
		let mtype = Object.keys(copy.message)[0]
		let isEphemeral = mtype === 'ephemeralMessage'
        if (isEphemeral) {
            mtype = Object.keys(copy.message.ephemeralMessage.message)[0]
        }
        let msg = isEphemeral ? copy.message.ephemeralMessage.message : copy.message
		let content = msg[mtype]
        if (typeof content === 'string') msg[mtype] = text || content
		else if (content.caption) content.caption = text || content.caption
		else if (content.text) content.text = text || content.text
		if (typeof content !== 'string') msg[mtype] = {
			...content,
			...options
        }
        if (copy.key.participant) sender = copy.key.participant = sender || copy.key.participant
		else if (copy.key.participant) sender = copy.key.participant = sender || copy.key.participant
		if (copy.key.remoteJid.includes('@s.whatsapp.net')) sender = sender || copy.key.remoteJid
		else if (copy.key.remoteJid.includes('@broadcast')) sender = sender || copy.key.remoteJid
		copy.key.remoteJid = jid
		copy.key.fromMe = sender === KUMUTHU.user.id

        return proto.WebMessageInfo.fromObject(copy)
    }


//send 5 button image by xeon
    KUMUTHU.send5ButImg = async (jid , text = '' , footer = '', img, but = [], thumb, options = {}) =>{
        let message = await prepareWAMessageMedia({ image: img, jpegThumbnail:thumb }, { upload: KUMUTHU.waUploadToServer })
        var template = generateWAMessageFromContent(m.chat, proto.Message.fromObject({
        templateMessage: {
        hydratedTemplate: {
        imageMessage: message.imageMessage,
               "hydratedContentText": text,
               "hydratedFooterText": footer,
               "hydratedButtons": but
            }
            }
            }), options)
            KUMUTHU.relayMessage(jid, template.message, { messageId: template.key.id })
    }


    //send5butvid by xeon
        KUMUTHU.send5ButVid = async (jid , text = '' , footer = '', vid, but = [], options = {}) =>{
        let message = await prepareWAMessageMedia({ video: vid }, { upload: KUMUTHU.waUploadToServer })
        var template = generateWAMessageFromContent(jid, proto.Message.fromObject({
        templateMessage: {
        hydratedTemplate: {
        videoMessage: message.videoMessage,
               "hydratedContentText": text,
               "hydratedFooterText": footer,
               "hydratedButtons": but
            }
            }
            }), options)
            KUMUTHU.relayMessage(jid, template.message, { messageId: template.key.id })
    }
    
    
    //send5butmsg by xeon
            KUMUTHU.send5ButMsg = (jid, text = '' , footer = '', but = []) =>{
        let templateButtons = but
        var templateMessage = {
        text: text,
        footer: footer,
        templateButtons: templateButtons
        }
        KUMUTHU.sendMessage(jid, templateMessage)
        }


//sendlistmsg by xeon
        KUMUTHU.sendListMsg = (jid, text = '', footer = '', title = '' , butText = '', sects = [], quoted) => {
        let sections = sects
        var listMes = {
        text: text,
        footer: footer,
        title: title,
        buttonText: butText,
        sections
        }
        KUMUTHU.sendMessage(jid, listMes, { quoted: quoted })
        }


    //send5butgif by xeon
        KUMUTHU.send5ButGif = async (jid , text = '' , footer = '', gif, but = [], options = {}) =>{
        let message = await prepareWAMessageMedia({ video: gif, gifPlayback: true }, { upload: KUMUTHU.waUploadToServer })
        var template = generateWAMessageFromContent(jid, proto.Message.fromObject({
        templateMessage: {
        hydratedTemplate: {
        videoMessage: message.videoMessage,
               "hydratedContentText": text,
               "hydratedFooterText": footer,
               "hydratedButtons": but
            }
            }
            }), options)
            KUMUTHU.relayMessage(jid, template.message, { messageId: template.key.id })
    }


    /**
     * 
     * @param {*} path 
     * @returns 
     */
    KUMUTHU.getFile = async (PATH, save) => {
        let res
        let data = Buffer.isBuffer(PATH) ? PATH : /^data:.*?\/.*?;base64,/i.test(PATH) ? Buffer.from(PATH.split`,`[1], 'base64') : /^https?:\/\//.test(PATH) ? await (res = await getBuffer(PATH)) : fs.existsSync(PATH) ? (filename = PATH, fs.readFileSync(PATH)) : typeof PATH === 'string' ? PATH : Buffer.alloc(0)
        //if (!Buffer.isBuffer(data)) throw new TypeError('Result is not a buffer')
        let type = await FileType.fromBuffer(data) || {
            mime: 'application/octet-stream',
            ext: '.bin'
        }
        filename = path.join(__filename, '../Android/src/' + new Date * 1 + '.' + type.ext)
        if (data && save) fs.promises.writeFile(filename, data)
        return {
            res,
            filename,
	    size: await getSizeMedia(data),
            ...type,
            data
        }

    }

    return KUMUTHU
}

startKUMUTHU()


let file = require.resolve(__filename)
fs.watchFile(file, () => {
	fs.unwatchFile(file)
	console.log(chalk.redBright(`Update ${__filename}`))
	delete require.cache[file]
	require(file)
})
